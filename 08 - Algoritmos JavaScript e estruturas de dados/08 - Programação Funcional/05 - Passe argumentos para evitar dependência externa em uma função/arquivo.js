// The global variable
let fixedValue = 4;

// Only change code below this line
function incrementer(incrementedValue) {
    // Only change code above this line
    incrementedValue = fixedValue + 1
    return incrementedValue
}